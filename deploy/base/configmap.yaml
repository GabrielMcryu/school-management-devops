apiVersion: v1
kind: ConfigMap
metadata:
  name: school-config
  namespace: school
data:
  DEBUG: "False"
  ALLOWED_HOSTS: "*"
  CSRF_TRUSTED_ORIGINS: "http://localhost,http://school.local"
  # Deployed frontend origin(s) allowed to call the API from the browser.
  # Scheme-qualified, no trailing slash, comma-separated for multiple.
  CORS_ALLOWED_ORIGINS: "https://schoolfe308752.z13.web.core.windows.net"
  POSTGRES_HOST: "postgres"
  POSTGRES_PORT: "5432"
