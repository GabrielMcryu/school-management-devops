apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - storage-class-patch.yaml

patches:
  - path: storage-class-patch.yaml
    target:
      kind: StatefulSet
      name: postgres

images:
  - name: ghcr.io/gabrielmcryu/school-management-devops
    newTag: latest
